# Contributing to hydrocomplex

- Follow the code style (ruff/isort).
- Add unit tests for new features.
- Use the ISSUE_TEMPLATE that best fits your contribution.
- For case studies, include a YAML config and a small reproducible dataset.
- Run tests locally: `pip install -e .[test]` then `pytest`.
