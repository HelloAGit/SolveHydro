# Flood forecasting case study

This folder contains a minimal reproducible flood forecasting example:
- `config.yaml` describes inputs and parameters
- `sample_data.csv` contains a tiny synthetic rainfall time series
- Notebook (to be added) demonstrates end-to-end: preprocess -> runoff -> routing -> metrics
